<form method="post" action="{{ path('app_product_option_value_delete', {'id': product_option_value.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer cette entrée ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_option_value.id) }}">
    <button class="btn">Supprimer</button>
</form>
